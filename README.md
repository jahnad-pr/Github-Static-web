# GitHub Static Web

A static GitHub homepage clone — plain HTML, CSS, and JS, no build tools.

## Run Locally

Just open `index.html` in your browser. No install needed.

Or serve it locally:
```bash
git clone https://github.com/Jahanad-pr/Github-Static-web_.git
cd Github-Static-web_
npx serve .
```

